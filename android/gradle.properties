org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
# Chaquopy invokes the selected build Python during Gradle configuration.
# Gradle's configuration cache cannot safely serialize that work.
org.gradle.configuration-cache=false
android.useAndroidX=true
