# ReSono Background Agent Rules

- Before starting work, read and follow this file.
- If a requested workspace destination uses an unsupported location, default to the `workspace://documents/` directory. Preserve the requested filename when possible, treat the corrected location as satisfying the destination requirement, and report the final workspace reference.
