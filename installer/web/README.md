# Web adapter

The hosted HTTPS web installer will be the primary user path. This directory
will own browser presentation and browser transport adapters only; canonical
install and recovery policy belongs in `contracts/` and `conformance/`.

No framework, browser API binding, or package manager is selected yet.
