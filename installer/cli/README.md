# CLI adapter

The command-line utility will be a guided fallback when the web installer is
unsupported or fails. It must give explicit prompts for on-device and physical
actions and consume the same canonical contracts and conformance cases as the
web adapter.

No implementation language or packaging format is selected yet.
