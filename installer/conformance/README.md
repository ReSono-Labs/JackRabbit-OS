# Conformance

Shared black-box installer cases belong here. Every supported web and CLI
adapter must prove the same safety, verification, interruption, and recovery
rules before release.

Fixtures use deterministic non-release identities and bytes. They prove offline
contract and policy behavior only; they are never presented as a device or
install result.
