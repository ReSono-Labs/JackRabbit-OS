# Web

This directory owns JackRabbit's real browser management surfaces and shared design vocabulary.

- `design/` contains semantic tokens and base behavior.
- `management/` contains the paired, live device configuration and runtime surface.

The Android HTTPS transport packages these files unchanged. UI state must come from a working runtime endpoint; mock data, placeholder controls, and future-feature navigation are prohibited.
